{
  "config": {
    "step": {
      "user": {
        "title": "MeshCore UI",
        "description": "Add the MeshCore UI dashboard panel to Home Assistant.\n\nMeshCore integration status: {meshcore_status}\n\nNote: meshcore-ha must be installed and configured first.",
        "data": {}
      }
    },
    "abort": {
      "single_instance_allowed": "Only one MeshCore UI instance is allowed.",
      "already_configured": "MeshCore UI is already configured."
    },
    "error": {
      "meshcore_not_found": "The meshcore integration was not found. Please install and configure meshcore-ha first, then try again."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "MeshCore UI Options",
        "data": {
          "message_limit": "Message history limit per conversation"
        }
      }
    }
  }
}
